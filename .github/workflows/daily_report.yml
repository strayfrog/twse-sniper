name: Global Tactical Intelligence (Data & Report)

on:
  schedule:
    - cron: '30 7 * * *'    # 台灣 15:30 (台股)
    - cron: '30 0 * * 2-6'  # 台灣 08:30 (美股週二至週六)
  workflow_dispatch:

jobs:
  intelligence_mission:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          # 🚨 全面換裝新版 google-genai
          python -m pip install requests yfinance beautifulsoup4 pandas lxml google-api-python-client google-auth-httplib2 google-auth-oauthlib google-genai

      - name: 📈 第一階段：偵蒐開盤狀態與執行腳本
        env:
          GOOGLE_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
        run: |
          python - <<EOF
          import requests
          from datetime import datetime, timedelta, timezone
          import subprocess
          import sys

          tw_tz = timezone(timedelta(hours=8))
          today_str = datetime.now(tw_tz).strftime('%Y%m%d')
          print(f"--- 戰前偵蒐開始 (今日台灣時間: {today_str}) ---")

          try:
              resp = requests.get("
