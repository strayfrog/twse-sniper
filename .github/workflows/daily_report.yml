name: Global Tactical Intelligence (Data Fetch Only)

on:
  schedule:
    # 🚨 時間已校正：台股 15:50 (UTC 07:50)，美股 07:50 (UTC 23:50，因跨日故設定為美東隔日)
    - cron: '50 7 * * *'    # 台灣 15:50 (台股)
    - cron: '50 23 * * 1-5' # 台灣 07:50 (美股週二至週六，對應 UTC 週一至週五)
  workflow_dispatch:

jobs:
  intelligence_mission:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          # 🚨 拔除所有與 AI 相關的套件，只保留爬蟲與 GDrive 必需品
          python -m pip install requests yfinance beautifulsoup4 pandas lxml google-api-python-client google-auth-httplib2 google-auth-oauthlib

      - name: 📈 第一階段：偵蒐開盤狀態與擷取數據
        run: |
          python - << 'EOF'
          import requests
          from datetime import datetime, timedelta, timezone
          import subprocess
          import sys

          # 強制鎖定台灣時間
          tw_tz = timezone(timedelta(hours=8))
          today_str = datetime.now(tw_tz).strftime('%Y%m
